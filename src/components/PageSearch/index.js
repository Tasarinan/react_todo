import React from 'react';

const PageSearch = () => (
	<div className="PageSearch">
		<input type="search" name="page-search" id="page-search" placeholder="Search"/>
	</div>
);

export default PageSearch;
